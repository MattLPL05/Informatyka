#include <iostream>
using namespace std;

int main()
{
    int a;
    int b;
    int iloczyn; // Do NWW
    int nww; //Do NWW
    cout << "Podaj pierwszą liczbe" << endl;
    cin >> a;
    cout << "Podaj drugą liczbe" << endl;
    cin >> b;
    iloczyn = a * b; // Do NWW
    while (a != b)
        if (a < b)
            b = b - a;
        else
            a = a - b; 
    nww = iloczyn / a; // Do NWW
    cout << "Najmniejsza wspolna wielokrotnosc: ";
    cout << nww;
    cout << endl;

    cout << "Najwiekszy wspolny dzielnik: ";
    cout << a;
    cout << endl;

        
}
